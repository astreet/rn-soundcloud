/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import {
  AppRegistry,
} from 'react-native';
import SetsFinder from './SetsFinder';

AppRegistry.registerComponent('SoundCloud2', () => SetsFinder);
